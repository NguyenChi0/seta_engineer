PORT=4001
# MySQL: cau hinh trong db.js
JWT_SECRET=use_a_long_random_string_here
JWT_EXPIRES_IN=7d

# SEO — hostname chinh thuc (co www)
SITE_URL=https://www.seta-eng.co.jp

# Bat de Express phuc vu app/dist + pre-render HTML (production)
# SERVE_FRONTEND=1

# (tuy chon) vong so bcrypt; mac dinh 10
# BCRYPT_SALT_ROUNDS=10
